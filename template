<!DOCTYPE html>
<html>
<head>
    <title>Fleet Management System</title>
</head>
<body>
    <h1>Fleet Information</h1>
    <ul>
        {% for vehicle in fleet.vehicles %}
            <li>
                <strong>{{ vehicle.name }}</strong>
                <ul>
                    <li>Driver: {{ vehicle.driver_name }}</li>
                    <li>Total Mileage: {{ vehicle.mileage }}</li>
                    <li>Remaining Mileage to Service: {{ vehicle.mileage_limit - vehicle.mileage }}</li>
                    <li>Service Count: {{ vehicle.service_count }}</li>
                    <li>Error Count: {{ vehicle.error_count }}</li>
                    <li>Total Repair Cost: {{ vehicle.total_repair_cost }}</li>
                    {% if vehicle.error_count > 0 %}
                        <li>Errors:
                            <ul>
                                {% for error in vehicle.repair_history %}
                                    <li>{{ error }}</li>
                                {% endfor %}
                            </ul>
                        </li>
                    {% endif %}
                </ul>
            </li>
        {% endfor %}
    </ul>
    <p>Overall Monthly Spending: {{ total_spending }}</p>
</body>
</html>
